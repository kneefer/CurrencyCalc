<UserControl x:Class="CurrencyCalc.Pages.Live"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="439" d:DesignWidth="639"
             DataContext="{Binding Live, Source={StaticResource ViewModelLocator}}"
             xmlns:mui="http://firstfloorsoftware.com/ModernUI">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0" Text="{Binding InputMoney}" TextAlignment="Center" FontSize="38" />
            <Button Grid.Column="1" Command="{Binding ReverseCurrenciesCommand}">
                <Image Source="../Images/swap.png"  />
            </Button>
            <TextBox IsEnabled="False" Grid.Column="2" Text="{Binding OutputMoney}" FontSize="38" TextAlignment="Center" />
        </Grid>
    </Grid>

</UserControl>
